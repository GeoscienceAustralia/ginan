
inputs:
    include_yamls:
        - tiny_post_network.yaml
        - static_user.yaml
        - mongo_outputs.yaml
        - example_inputs.yaml

outputs:
    metadata:
        config_description:     "ex201"
