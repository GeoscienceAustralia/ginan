
inputs:

    gnss_observations:
        inputs_root:    "https://<USER>:<PASS>@ntrip.data.gnss.ga.gov.au/"
        rtcm_inputs:
            - "ALIC00AUS0"  
            - "COCO00AUS0"  
            - "LHAZ00CHN0"  
            - "KZN200RUS0"  
        
    satellite_data:
        inputs_root:    "https://<USER>:<PASS>@ntrip.data.gnss.ga.gov.au/"
        rtcm_inputs:
            - "BCEP00BKG0"
            - "SSRA00BKG0"
            
processing_options:

    ssr_inputs:
        ssr_antenna_offset: APC
